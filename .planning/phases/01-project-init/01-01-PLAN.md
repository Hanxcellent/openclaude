---
phase: 01-project-init
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PromptInput/KeepMounted.tsx
  - src/components/PromptInput/PromptInputFooter.tsx
  - src/components/PromptInput/PromptInputFooterLeftSide.tsx
  - src/components/StatusLine.tsx
  - src/components/PromptInput/KeepMounted.test.tsx
  - src/components/PromptInput/PromptInputFooter.test.tsx
  - src/components/PromptInput/PromptInputFooterLeftSide.test.ts
  - src/components/StatusLine.test.ts
autonomous: true
requirements: [REQ-001, REQ-002, REQ-003, REQ-004, REQ-005, REQ-006, REQ-007, REQ-008, REQ-009, REQ-020, REQ-023]
user_setup: []
domain_trace:
  interaction_gate: required
  use_cases: [UC-001, UC-003, UC-004, UC-008, UC-009]
  actors: [CLI Operator]
  concepts: [Prompt Session, Transient Prompt Surface, Footer Service, Custom Statusline Update]
  derived_access_rules: ["allowed: CLI Operator may perform all covered prompt operations"]
must_haves:
  truths:
    - Transient footer visibility never remounts long-lived footer children.
    - Hidden statusline state owns no pending debounce timer or in-flight command.
    - First activation and dirty reactivation each execute exactly one immediate update.
    - Visible ordinary state changes produce at most one debounced update.
  artifacts:
    - src/components/PromptInput/KeepMounted.tsx
    - src/components/StatusLine.tsx
    - src/components/StatusLine.test.ts
  key_links:
    - PromptInputFooter resolves mounted, visible, and active statusline state in one operation.
    - StatusLine active state drives a single scheduling state machine.
---

> **Superseded combined plan**: Do not use this file as a review or execution unit. IR-001 and IR-002 are independently specified in `.planning/requirements/01-slash-suggestion-freeze.md` and `.planning/requirements/02-ctrl-c-feedback-freeze.md`, each directly based on `main`.

<objective>
Stabilize prompt footer component lifecycles and make custom statusline side effects explicitly suspendable.

Purpose: Remove remount and hidden-command stalls around `/`, help, paste, and Ctrl+C transitions.
Output: A reusable mounted-hidden container, unified footer state resolver, explicit statusline state machine, and direct regression tests.
</objective>

<execution_context>
@workflows/execute-plan.md
@templates/SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/DOMAIN.md
@.planning/USE_CASES.md
@.planning/REQUIREMENTS.md
@.planning/codebase/architecture.md
@.planning/codebase/conventions.md
@src/components/PromptInput/PromptInputFooter.tsx
@src/components/PromptInput/PromptInputFooterLeftSide.tsx
@src/components/StatusLine.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep footer children mounted while hidden</name>
  <domain_trace>
    <actors>CLI Operator</actors>
    <use_cases>UC-001, UC-003, UC-004</use_cases>
    <concepts>Prompt Session, Transient Prompt Surface, Footer Service</concepts>
    <derived_access></derived_access>
  </domain_trace>
  <files>src/components/PromptInput/KeepMounted.tsx, src/components/PromptInput/PromptInputFooter.tsx, src/components/PromptInput/PromptInputFooterLeftSide.tsx</files>
  <read_first>src/components/PromptInput/PromptInputFooter.tsx, src/components/PromptInput/PromptInputFooterLeftSide.tsx, src/ink/components/Box.tsx</read_first>
  <action>Create a small KeepMounted component that collapses children with height 0 and overflow clipping without changing child identity. Replace early-return or conditional-unmount paths for regular footer content and transient left-side feedback. Keep permanent guards such as non-prompt mode and short fullscreen allowed to avoid mounting services that can never display.</action>
  <verify>bun test src/components/PromptInput/KeepMounted.test.tsx src/components/PromptInput/PromptInputFooter.test.tsx src/components/PromptInput/PromptInputFooterLeftSide.test.ts</verify>
  <acceptance_criteria>
    - KeepMounted children mount once across visible-hidden-visible renders and unmount only with the parent.
    - Hidden content adds no blank row before visible siblings.
    - Visible wrapped content is not clipped.
    - History-search focus is false while its footer content is hidden.
  </acceptance_criteria>
  <done>Lifecycle and layout tests pass for both regular footer replacement and transient left-side feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Unify footer statusline guards</name>
  <domain_trace>
    <actors>CLI Operator</actors>
    <use_cases>UC-001, UC-003, UC-008</use_cases>
    <concepts>Footer Service, Custom Statusline Update</concepts>
    <derived_access></derived_access>
  </domain_trace>
  <files>src/components/PromptInput/PromptInputFooter.tsx, src/components/PromptInput/PromptInputFooter.test.tsx</files>
  <read_first>src/components/PromptInput/PromptInputFooter.tsx, src/components/StatusLine.tsx, src/components/BuiltinStatusLine.tsx</read_first>
  <action>Replace overlapping mounted, visible, and active predicates with one resolver that returns all three values from the same guard input. A custom statusline remains mounted across transient exit/paste/help/suggestion states, is visible only when row guards pass, and is active only when both visible and the regular footer is not hidden.</action>
  <verify>bun test src/components/PromptInput/PromptInputFooter.test.tsx</verify>
  <acceptance_criteria>
    - A single exported resolver returns mounted, visible, and customActive state.
    - Tests cover permanent guards, exit/paste guards, and suggestion/help hiding.
    - No second predicate duplicates the activation rules.
  </acceptance_criteria>
  <done>Footer statusline behavior has one source of truth and all guard combinations are deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Implement explicit statusline scheduling state machine</name>
  <domain_trace>
    <actors>CLI Operator</actors>
    <use_cases>UC-008, UC-009</use_cases>
    <concepts>Custom Statusline Update</concepts>
    <derived_access></derived_access>
  </domain_trace>
  <files>src/components/StatusLine.tsx, src/components/StatusLine.test.ts</files>
  <read_first>src/components/StatusLine.tsx, src/utils/hooks.ts</read_first>
  <action>Represent statusline transitions as a pure function yielding cancel, run, schedule, or none. Use one effect for initial activation, active changes, message/model/permission/vim changes, and command hot reload. On deactivation clear debounce timers and abort in-flight commands. Guard doUpdate and scheduleUpdate with current active state. Preserve one dirty bit so hidden changes result in exactly one immediate update on reactivation.</action>
  <verify>bun test src/components/StatusLine.test.ts</verify>
  <acceptance_criteria>
    - Hidden pending work transitions to cancel and dirty.
    - First activation with changes transitions to one run, not run plus schedule.
    - Dirty reactivation transitions to one run.
    - Ordinary visible changes transition to schedule.
    - doUpdate cannot start while activeRef is false.
  </acceptance_criteria>
  <done>Statusline correctness is independent of effect order and all transition tests pass.</done>
</task>

</tasks>

<verification>
- `bun run typecheck`
- `bun test src/components/StatusLine.test.ts src/components/PromptInput/KeepMounted.test.tsx src/components/PromptInput/PromptInputFooter.test.tsx src/components/PromptInput/PromptInputFooterLeftSide.test.ts`
- `bun run build`
</verification>

<success_criteria>
- All must_haves are satisfied.
- No transient footer path conditionally removes long-lived services.
- Hidden statusline commands cannot start or remain in flight.
- Focused tests and build pass without new dependencies.
</success_criteria>
