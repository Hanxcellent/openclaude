---
phase: 03-verification
plan: 01
type: execute
wave: 1
depends_on: ["01-01", "02-01"]
files_modified:
  - .planning/phases/03-verification/03-VALIDATION.md
  - .planning/phases/03-verification/03-UAT.md
autonomous: false
requirements: [REQ-017, REQ-018, REQ-019, REQ-020, REQ-021, REQ-022, REQ-023]
user_setup:
  - Run manual TUI UAT in a real terminal using the development build and an enabled custom statusline.
domain_trace:
  interaction_gate: required
  use_cases: [UC-001, UC-002, UC-003, UC-004, UC-005, UC-006, UC-007, UC-008, UC-009]
  actors: [CLI Operator]
  concepts: [Prompt Session, Footer Service, Custom Statusline Update, File Suggestion Index, Index Refresh, Lifecycle Subscription]
  derived_access_rules: ["allowed: CLI Operator may execute and report all covered UAT operations"]
must_haves:
  truths:
    - All focused regression tests pass.
    - Typecheck and production build pass.
    - Full-suite failure names are a subset of or equal to the `main` baseline; implementation-only failures are forbidden while resolved baseline failures are allowed.
    - Representative prompt transitions produce no frame at or above 16ms on the development machine.
    - Manual typing remains visible during every formerly stalling transition.
  artifacts:
    - .planning/phases/03-verification/03-VALIDATION.md
    - .planning/phases/03-verification/03-UAT.md
  key_links:
    - Every v1 requirement maps to an automated check or UAT case.
    - Full-suite output is compared by normalized failure name, not only count; pass totals may differ when regression tests are added.
---

<objective>
Close the fix with reproducible automated, performance, integration, and manual evidence.

Purpose: Prevent a locally smooth implementation from shipping with lifecycle, freshness, or baseline regressions.
Output: Completed validation evidence and user-confirmed UAT results.
</objective>

<execution_context>
@workflows/execute-plan.md
@workflows/verify-work.md
@templates/SUMMARY.md
@references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-verification/03-VALIDATION.md
@.planning/phases/03-verification/03-UAT.md
@.planning/codebase/testing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run focused correctness gates</name>
  <domain_trace>
    <actors>CLI Operator</actors>
    <use_cases>UC-001, UC-002, UC-003, UC-004, UC-005, UC-007, UC-008, UC-009</use_cases>
    <concepts>Prompt Session, Footer Service, Custom Statusline Update, File Suggestion Index, Lifecycle Subscription</concepts>
    <derived_access>allowed: CLI Operator may execute automated validation</derived_access>
  </domain_trace>
  <files>.planning/phases/03-verification/03-VALIDATION.md</files>
  <read_first>.planning/phases/01-project-init/01-01-PLAN.md, .planning/phases/02-file-index/02-01-PLAN.md, package.json</read_first>
  <action>Run typecheck, build, and all focused lifecycle/index tests listed in 03-VALIDATION.md. Record exact commands, pass counts, failures, and environment date. A focused failure blocks UAT.</action>
  <verify>All focused commands exit 0.</verify>
  <acceptance_criteria>
    - Validation report records command output summaries.
    - Every REQ-020 behavior has a named passing test.
  </acceptance_criteria>
  <done>Focused automated gates are green and traceable.</done>
</task>

<task type="auto">
  <name>Task 2: Compare full check against main baseline</name>
  <domain_trace>
    <actors>CLI Operator</actors>
    <use_cases>UC-001, UC-002, UC-003, UC-004, UC-005, UC-006, UC-007, UC-008, UC-009</use_cases>
    <concepts>Prompt Session, Footer Service, File Suggestion Index, Index Refresh</concepts>
    <derived_access>allowed: CLI Operator may review regression evidence</derived_access>
  </domain_trace>
  <files>.planning/phases/03-verification/03-VALIDATION.md</files>
  <read_first>.planning/codebase/testing.md, package.json</read_first>
  <action>Run `bun run check` on the implementation and on the exact `main` baseline in an isolated worktree using the same dependency installation and environment. Normalize `(fail)` names by removing durations, deduplicate and sort them, then compare sets. Record pass/skip/fail totals and both set differences. Added regression tests may change pass totals; existing baseline failures may disappear.</action>
  <verify>`only_fix=0`; fix failure count is not greater than main failure count.</verify>
  <acceptance_criteria>
    - Smoke and deadcode results are recorded.
    - Full-suite totals are recorded for both refs.
    - Failure-name set comparison has zero implementation-only failures.
    - A lower implementation failure count is accepted only when every remaining failure name belongs to the main baseline.
  </acceptance_criteria>
  <done>No new full-suite regression is attributable to this fix.</done>
</task>

<task type="auto">
  <name>Task 3: Capture frame and command-execution evidence</name>
  <domain_trace>
    <actors>CLI Operator</actors>
    <use_cases>UC-001, UC-002, UC-003, UC-008, UC-009</use_cases>
    <concepts>Prompt Session, Custom Statusline Update, Index Refresh</concepts>
    <derived_access>allowed: CLI Operator may execute performance diagnostics</derived_access>
  </domain_trace>
  <files>.planning/phases/03-verification/03-VALIDATION.md</files>
  <read_first>src/components/StatusLine.tsx, src/hooks/fileSuggestions.ts, src/ink/log-update.ts</read_first>
  <action>Run the built CLI with frame timing and debug logs enabled. Trigger `/` disappearance, `@` disappearance during a five-second refresh, and Ctrl+C feedback expiration while typing. Record maximum total/render/diff/write/Yoga frame duration, statusline command completion count, and tracked-file scan count.</action>
  <verify>Maximum total frame is below 16ms; no duplicate statusline command or duplicate refresh is observed for one transition.</verify>
  <acceptance_criteria>
    - Timing log contains numeric maxima.
    - Debug log demonstrates single-flight scans and no hidden statusline execution.
  </acceptance_criteria>
  <done>Runtime evidence shows no event-loop-scale stall.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <domain_trace>
    <actors>CLI Operator</actors>
    <use_cases>UC-001, UC-002, UC-003, UC-004, UC-005, UC-006, UC-007</use_cases>
    <concepts>Prompt Session, Transient Prompt Surface, File Suggestion Index, Lifecycle Subscription</concepts>
    <derived_access>allowed: CLI Operator may perform all UAT operations</derived_access>
  </domain_trace>
  <what-built>Development CLI with prompt lifecycle and progressive file-index corrections.</what-built>
  <how-to-verify>Run every case in `.planning/phases/03-verification/03-UAT.md` in a real terminal. Enter pass/fail plus observed delay for each case. Any recurrence of buffered typing is a blocking failure.</how-to-verify>
  <resume-signal>Reply with UAT results for UAT-01 through UAT-07.</resume-signal>
</task>

</tasks>

<verification>
- Automated checks in `03-VALIDATION.md`
- Human checks in `03-UAT.md`
- Requirement-to-evidence matrix has no uncovered v1 requirement
</verification>

<success_criteria>
- Automated validation is passed or contains only documented `main` baseline failures.
- All UAT cases pass.
- No freshness trade-off was introduced.
- No unresolved high- or medium-severity gap remains.
</success_criteria>
