---
phase: 02-file-index
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/fileSuggestions.ts
  - src/hooks/useTypeahead.tsx
  - src/components/QuickOpenDialog.tsx
  - src/hooks/fileSuggestions.test.ts
  - src/hooks/useTypeahead.test.ts
  - src/components/QuickOpenDialog.test.ts
autonomous: true
requirements: [REQ-010, REQ-011, REQ-012, REQ-013, REQ-014, REQ-015, REQ-016, REQ-018, REQ-019, REQ-020, REQ-023]
user_setup: []
domain_trace:
  interaction_gate: required
  use_cases: [UC-002, UC-005, UC-006, UC-007]
  actors: [CLI Operator]
  concepts: [File Suggestion Index, Index Refresh, Lifecycle Subscription]
  derived_access_rules: ["allowed: CLI Operator may perform all covered file suggestion operations"]
must_haves:
  truths:
    - The first and subsequent file queries never await complete repository discovery.
    - Refresh completion cannot reenter and start a duplicate scan.
    - Cache reset preserves mounted lifecycle subscribers.
    - Five-second untracked-file discovery remains enabled.
    - Typeahead and Quick Open rerun only active queries after index completion without reopening accepted, suppressed, or dismissed suggestions.
    - Quick Open completion replay does not start another background refresh.
    - Typeahead preserves the selected partial result while merging complete-index results.
  artifacts:
    - src/hooks/fileSuggestions.ts
    - src/hooks/useTypeahead.tsx
    - src/hooks/fileSuggestions.test.ts
  key_links:
    - Typeahead mount starts prewarm and subscribes to completion.
    - File-index completion publishes committed refresh state before notifying listeners.
    - Cache reset clears data but not the completion signal's listeners.
---

> **Independent execution unit**: This plan implements only IR-003 from `main`; it does not depend on IR-001 or IR-002. `.planning/requirements/03-at-suggestion-freeze.md` is the review authority.

<objective>
Move repository discovery off the keyboard response path while preserving freshness and automatic result upgrades.

Purpose: Eliminate `@` stalls without accepting stale untracked-file suggestions.
Output: Progressive prewarm, non-blocking queries, reentrancy-safe completion, persistent lifecycle subscriptions, and controlled process tests.
</objective>

<execution_context>
@workflows/execute-plan.md
@templates/SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/DOMAIN.md
@.planning/USE_CASES.md
@.planning/REQUIREMENTS.md
@.planning/codebase/concerns.md
@.planning/codebase/testing.md
@src/hooks/fileSuggestions.ts
@src/hooks/useTypeahead.tsx
@src/components/QuickOpenDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prewarm and query the progressive index</name>
  <domain_trace>
    <actors>CLI Operator</actors>
    <use_cases>UC-002, UC-005</use_cases>
    <concepts>File Suggestion Index, Index Refresh</concepts>
    <derived_access></derived_access>
  </domain_trace>
  <files>src/hooks/useTypeahead.tsx, src/hooks/fileSuggestions.ts, src/hooks/useTypeahead.test.ts, src/hooks/fileSuggestions.test.ts</files>
  <read_first>src/hooks/useTypeahead.tsx, src/hooks/fileSuggestions.ts, src/native-ts/file-index/index.ts</read_first>
  <action>Start built-in file-index refresh when typeahead mounts outside tests. Do not start it for command-backed custom file suggestions. Remove any cold-query await of the full refresh promise; query the ready prefix immediately and let completion update results later.</action>
  <verify>bun test src/hooks/useTypeahead.test.ts src/hooks/fileSuggestions.test.ts</verify>
  <acceptance_criteria>
    - Production initialization invokes startBackgroundCacheRefresh once and registers a completion listener.
    - Test mode skips real prewarm but still permits listener testing.
    - Command-backed file suggestions neither prewarm nor subscribe to the built-in index.
    - A controlled slow Git process remains unresolved while generateFileSuggestions has already returned.
  </acceptance_criteria>
  <done>Cold `@` queries are non-blocking and prewarm behavior is directly tested.</done>
</task>

<task type="auto">
  <name>Task 2: Make refresh completion reentrancy-safe</name>
  <domain_trace>
    <actors>CLI Operator</actors>
    <use_cases>UC-002</use_cases>
    <concepts>Index Refresh, Lifecycle Subscription</concepts>
    <derived_access></derived_access>
  </domain_trace>
  <files>src/hooks/fileSuggestions.ts, src/hooks/fileSuggestions.test.ts</files>
  <read_first>src/hooks/fileSuggestions.ts, src/utils/signal.ts</read_first>
  <action>On successful refresh, commit `.git/index` observation, freshness timestamp, and single-flight promise state before emitting indexBuildComplete. Verify a subscriber that synchronously requests another refresh cannot start a duplicate tracked-file scan.</action>
  <verify>bun test src/hooks/fileSuggestions.test.ts</verify>
  <acceptance_criteria>
    - Completion state is visible before listeners run.
    - A controlled listener reentry leaves tracked spawn count at one.
    - The five-second throttle remains present.
  </acceptance_criteria>
  <done>Index completion cannot cause duplicate repository discovery.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve subscriptions across cache reset and refresh consumers</name>
  <domain_trace>
    <actors>CLI Operator</actors>
    <use_cases>UC-007, UC-002</use_cases>
    <concepts>Lifecycle Subscription, File Suggestion Index</concepts>
    <derived_access></derived_access>
  </domain_trace>
  <files>src/hooks/fileSuggestions.ts, src/hooks/useTypeahead.tsx, src/components/QuickOpenDialog.tsx, src/hooks/fileSuggestions.test.ts, src/components/QuickOpenDialog.test.ts</files>
  <read_first>src/commands/clear/caches.ts, src/hooks/fileSuggestions.ts, src/hooks/useTypeahead.tsx, src/components/QuickOpenDialog.tsx</read_first>
  <action>Keep indexBuildComplete listeners across clearFileSuggestionCaches because they are component lifecycle state. Continue clearing index data, promises, generations, timestamps, signatures, and ignore caches. Ensure typeahead and Quick Open retain stable subscriptions and rerun only active queries after completion. Invalidate typeahead replay on suppression, dismissal, and completed acceptance; preserve the current selection during progressive result upgrades; make Quick Open completion replay query the committed index without starting another refresh.</action>
  <verify>bun test src/hooks/fileSuggestions.test.ts src/hooks/useTypeahead.test.ts src/components/QuickOpenDialog.test.ts</verify>
  <acceptance_criteria>
    - A listener registered before clearFileSuggestionCaches receives the next completion event.
    - `/clear` does not require PromptInput remount to recover `@` refresh behavior.
    - Quick Open reruns its current non-empty query on completion without starting a new refresh.
    - Ctrl+R history search, Escape dismissal, and completed Enter/Tab acceptance invalidate pending replay.
    - A selected partial result remains selected after complete-index results arrive.
  </acceptance_criteria>
  <done>All index consumers self-heal after cache reset without another keystroke.</done>
</task>

</tasks>

<verification>
- `bun run typecheck`
- `bun test src/hooks/fileSuggestions.test.ts src/hooks/useTypeahead.test.ts src/components/QuickOpenDialog.test.ts`
- `bun run build`
</verification>

<success_criteria>
- All must_haves are satisfied.
- Five-second freshness behavior is demonstrably retained.
- No file query awaits the full refresh promise.
- Controlled child-process tests pass without leaking background work.
</success_criteria>
